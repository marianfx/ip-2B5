package buildingParts

/**
 * Created by Admin on 21.05.2016.
 */
class BuildingPartTest extends GroovyTestCase {
    void testGetStart() {

    }

    void testGetEnd() {

    }/*
    @Test
    void testEquals() {
        // BuildingPart is tested
        Coordinates start = new Coordinates(2,3);
        Coordinates end = new Coordinates(4,3);

        BuildingPart tester = new BuildingPart(start, end);

        Coordinates newStart = tester.getStart();
        Coordinates newEnd = tester.getEnd();

        Coordinates otherStart = new Coordinates(10, 25);
        BuildingPart tester2 = new BuildingPart(otherStart, end);


        // assert statements
        assertEquals("start must be identical with no set used", start, newStart);
        assertEquals("end must be identical with no set used", end, newEnd);

        //building parts not equal
        assertEquals("different building parts must not be equal", false, tester.equals(tester2))
    }*/
}
/*

import static org.junit.Assert.assertEquals;

import org.junit.Test;

public class BuildingPartTest {


 */